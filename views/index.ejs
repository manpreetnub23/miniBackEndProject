<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="main w-full min-h-screen bg-zinc-900">
      <div class="form p-10 text-white">
        <form action="/create" method="post">
          <input
            name="title"
            type="text"
            class="block px-5 py-2 bg-zinc-800 w-full rounded-lg outline-none"
            placeholder="enter title.."
          />
          <textarea
            name="details"
            class="block mt-2 px-5 py-2 bg-zinc-800 resize-none w-full rounded-lg outline-none"
            placeholder="enter description of your task.."
          ></textarea>
          <center>
            <input
              type="submit"
              class="mt-2 p-3 w-1/2 rounded-full bg-blue-600"
              value="Create task"
            />
          </center>
        </form>
      </div>
      <div class="tasks p-10 flex gap-4 flex-wrap">
        <% if (files.length > 0) {%> <%files.forEach(function(val){%>
        <!-- task 1 -->
        <div class="task min-w-72 px-3 py-4 bg-zinc-800 rounded-lg">
          <h1 class="text-white text-2xl tracking-tighter"><%= val %></h1>
          <div class="flex items-center justify-between text-md text-zinc-400">
            <a
              href="/files/<%=val%>"
              class="text-blue-400 mt-3 hover:text-blue-500"
              >read more.</a
            >
            <a class="mt-3 hover:text-zinc-500" href="/edit/<%= val %>"
              >edit filename</a
            >
          </div>
        </div>
        <% })%> <%} else { %>
        <h3 class="text-gray text-2xl text-zinc-400">
          no tasks available yet.
        </h3>
        <%}%>
      </div>
    </div>
  </body>
</html>
